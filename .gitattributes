# Auto detect text files and perform LF normalization
* text=auto

# 셸 스크립트는 반드시 LF (리눅스에서 실행되므로)
*.sh text eol=lf
Dockerfile text eol=lf
Caddyfile text eol=lf

# 윈도우 배치 파일은 CRLF (더블클릭 실행)
*.bat text eol=crlf
*.cmd text eol=crlf
